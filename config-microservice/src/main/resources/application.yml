server:
  port: 8888

spring:
  application:
    name: config-serve
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
